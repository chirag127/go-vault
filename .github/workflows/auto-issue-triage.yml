name: auto-issue-triage
on:
  issues:
    types: [opened]
jobs:
  triage:
    uses: chirag127/workflows/.github/workflows/auto-issue-triage.yml@main
    with:
      components: "vault,cli,api,auth,docs,ci,deps"
    secrets: inherit
    permissions:
      issues: write
